# Walkthrough Punch List — 2026-07-21

## Round 2 (full re-walkthrough — log only, batch-fix at end)

| # | Page | Item | Notes | Status |
|---|------|------|-------|--------|

| 24 | Blog | Remove the AEO hub and all AEO posts | Inventory first: find the AEO hub (pillar page/landing) and every AEO post (search titles/slugs/categories/tags for AEO). Unpublish via the retired-pages pattern (set to draft — reversible — rather than hard delete; mirror in the seed/content-fixes draft pass so prod converges). Remove any nav/footer/blog-listing references and internal links pointing at them; add 301s only if they have real inbound links. Report the list of affected posts to Cole before/with the change. | ✅ |
| 23 | Agencies page — What We Do card icon + sweep | Bar-chart icon on one solutions card reads unrelated to the card's content | Cole flagged the ind-sol-icon (3-bar chart) on an agencies What We Do card as semantically unrelated. Fix: audit EVERY icon-to-content pairing across all 4 industry pages' What We Do/solutions cards + audience chips (icons come from the seed's icon slugs + stretch_industry_icon() library) and the home services grid; replace mismatches with semantically apt Lucide-style icons (add slugs to the library if missing). Report the final icon map per card. SECOND INSTANCE: shield icon on the agencies "White-Labeled…" card also flagged unrelated. THIRD INSTANCE: the SAME shield SVG appears again on a "Product & So…" card (different page/card) — the shield is evidently the icon library's DEFAULT/FALLBACK, so any card without an explicit slug inherits it. Root fix: assign explicit, semantically-apt slugs to every card in the seeds (no card left on the fallback), and make the fallback something neutral. FOURTH INSTANCE: layout/table icon flagged on another card. FIFTH+SIXTH: sparkle on an Expert-Written card; activity/pulse icon on an ABOUT-page value card — scope is now SITE-WIDE: every icon tile on every page (industry solution cards + audience chips, home services grid, About values/qualities grids, service why-cards, contact page, BCE per #16). Cole has flagged 6 — do NOT fix piecemeal; one audit re-maps them all and reports the final icon map. | ✅ |
| 22 | Ecommerce page — work gallery | Gallery images mismatched to their labels | Per Cole: (a) tile 2 "Lifestyle & Social" currently shows a Meyer's product flat-lay = product photography, wrong for the label — move the wave/surf image (currently tile 3) into the Lifestyle & Social slot; (b) the wave image is NOT campaign creative — source a real campaign-creative image for tile 3 (portfolio/media-library first: look for actual ad/campaign work; else stock). Retire the Meyer's flat-lay from tile 2 (tile 1 already covers product photography). Respect the #19 no-duplicate-images rule when picking. Update bundles + setup-page-images.php wiring. | ✅ |
| 21 | Visual page — Solution intro | Merge the two short intro sentences into ONE paragraph | Data change (setup-services.php, visual-content-and-design solution intro): "Effective visual content communicates, educates, and inspires action and loyalty." + "Stretch Creative helps businesses create visuals that support real marketing goals." → single paragraph, sentences joined with a space, copy otherwise untouched. While there: check the other 3 solution pages' Solution intros for the same two-stub-sentence pattern and flag (don't merge without asking unless visually identical situation). | ✅ |
| 20 | All hero sections | Double grid lines appear when the cursor moves (parallax) — revert to the old single-grid behavior | Root cause of the doubling: the #1 fix added a STATIC lattice (.pfx-hero-grid::before) on the wrapper, while the per-cell borders live on the parallax-translating .pfx-grid-container — two lattices slide over each other on mouse move. Fix: single line source that MOVES WITH the parallax layer — move the lattice gradient onto .pfx-grid-container (::before spanning its inset) and drop the per-cell borders, keeping cells only for colored pulses. Grid coverage must not regress (keep the hardened row math + full-coverage lattice) and grid must stay visible at rest (don't reintroduce #1). Verify by mouse-move screenshots on home + one service + one industry hero. | ✅ |
| 19 | SEO page — How We Work photo + SITE RULE | Duplicate image spotted (solution-process-seo-strategy.jpg); Cole's rule: never use the same image twice anywhere on the site | Replace the SEO process photo with a unique image. Then AUDIT SITE-WIDE: build the full list of rendered image URLs across every page (home, about, 4 solutions, 4 industries, contact, blog) and eliminate every duplicate — each photo appears exactly once site-wide. Check especially the visually-similar planning/meeting stock shots (easy accidental near-dupes) and the shared attachment IDs in stretch_page_images vs service/industry options. New picks follow the established bundle+sideload pattern. | ✅ |
| 18 | SITE-WIDE — all CTA buttons | ALL button text must render white (#fff) | Confirmed on BCE final CTA ("Get in touch →") AND BCE hero ("Let's Chat →") — page link-color rules override the kit button label color. Fix in the KIT with strong-enough specificity (.pfx-btn-primary, .pfx-btn-primary span, .pfx-btn-outline, :hover/:visited states → color:#fff) so no page-level `a` rule can override, then AUDIT every button on every page (home, 4 solutions, 4 industries, about, contact, blog, pricing, BCE) — computed color must be rgb(255,255,255) in both rest and hover states. | ✅ |
| 17 | Service + industry templates — enhancement | Add the BCE-style giant ghost section numbers to the other service/industry pages | Cole likes the large outline numeral ("04") top-right of the BCE dark Results section. Add the same treatment to major sections of page-service.php and page-industry.php: an oversized outline/ghost number (matching the BCE style — thin outline numeral, very low-contrast, top-right, behind content, aria-hidden) auto-numbered per rendered section. Keep it subtle on light sections (ghost #e-tier) and outline-on-dark like BCE. Should be generated from the section flow so numbering stays correct when optional sections skip. | ✅ |
| 16 | Interactive Content page — audience cards | Replace the ad-hoc inline SVG icons with real icons | The audience-card SVGs are nonsense shapes (e.g. one commented "Rocket" that draws a staircase). Replace every audience-icon (and sweep the whole BCE template for other junk inline SVGs) with proper Lucide icons — the site's established icon set from the redesign — at the system weight (24px grid, 1.7px stroke, round caps/joins, currentColor), semantically matched to each card's text. Echoes Cole's original audit complaint about AI-generated icons. | ✅ |
| 15 | Service pages — hero | Hero → props-band transition reads as a STRAIGHT horizontal line; the in-hero diagonal wedge is effectively invisible | Hero gradient ends ≈#1e2333 and the wedge fills #252C3A — nearly identical darks, so the diagonal doesn't read; the horizontal accent bar at the seam then dominates, making the boundary look straight. Fix: make the diagonal legible per design screenshots/02-solutions (raise contrast at the boundary — e.g. slightly lighter props band edge or subtle top highlight on the wedge) while keeping the accent bar. Applies to service AND industry hero variants — check both against the design captures. | ✅ |
| 14 | Home | Seam line still visible at the TOP edge of the stats→services wedge (dark-on-dark) | Round 1 fixed the wedge's BOTTOM edge (margin-bottom:-1px into the next section). This seam is the other boundary: previous section → wedge container. Fix: also overlap the top — margin-top:-1px on the wedge container (or equivalent) so both edges overlap; apply to the shared wedge classes in ALL templates (home-wedge, svc-wedge, ind-wedge, about wedges) and re-sweep every boundary at 100%/110%/125% zoom. SECOND INSTANCE: home Why Trust → final CTA purple wedge, same top-edge seam. The verification sweep must programmatically enumerate every wedge on every page (home, 4 solutions, 4 industries, about, BCE) and check BOTH edges. THIRD INSTANCE (SEO page): small grey hairline circled along the props→white wedge bottom-right — note this one sits along the clip-path DIAGONAL edge, so it may be the anti-aliasing hairline of the clipped triangle over a mismatched container bg (not just the horizontal top/bottom gap). The fix should also make the wedge container's own background match the preceding section so any AA sliver blends. FOURTH INSTANCE (ecommerce Why Stretch → FAQ wedge, top edge) — Cole: "ongoing issue through much of the site." Treat as systemic: fix the wedge pattern ONCE (shared approach across all templates: container bg = previous section color, triangle overlaps ±1px both edges), then screenshot-verify every single wedge boundary on all 11 pages before closing this item. | ✅ |
<!-- Round 2 entries appended here -->

## Round 1

Design-handoff rebuild review with Cole, page by page (localhost:8888).
Items logged as received; fixes batched at the end of the walkthrough.

Status legend: 🔴 logged · 🟡 in progress · ✅ fixed & verified

| # | Page | Item | Notes | Status |
|---|------|------|-------|--------|
| 6 | SEO page (data) | Move the lone "Budget management" add-on card into the card grid above as card #9 | Data-only change in setup-services.php (seo_content_strategy_services block): append {title: "Budget management", body: "Coordinated production oversight and budget tracking for larger SEO and content initiatives."} as the 9th item of the offerings/compact-card grid, remove it from `addons`. With Budget management moved up, the section below is repurposed: retitle it to the cross-sell pattern focused on more-than-SEO (like content-writing's "Need more than written content?" dashed card) with gradient-border pill links to the sibling solution pages. Update title + ensure pills point at content-writing / visual / paid / interactive. Re-apply seed locally; verify ghost number 09 renders on the moved card. | ✅ |
| 5 | SEO page (service template) | Solution-section spacing: gap between the two intro paragraphs is too large; last paragraph sits too close to the cards grid below | In page-service.php's "The Solution" section. Normalize intra-paragraph spacing (~18–22px, matching the design's lh/margins) and add proper separation (~40–48px) between the intro copy and the solution-cards grid. Template-level fix — verify on all 4 solution pages (content-writing, seo, paid, visual), since they share the section. Check the Reality section for the same pattern while in there. CONFIRMED on content-writing AND visual (Cole: "all pages have odd formatting here") — template-level. Root pattern visible in visual screenshot: two short intro paragraphs separated by a huge gap (each p likely carrying section-scale margins), then cramped against the grid. Normalize: ~18–22px between paragraphs, ~40–48px before the cards grid. | ✅ |
| 7 | Service template — testimonials | Equalize testimonial card heights | Grid items currently size to content. Fix in page-service.php CSS: cards fill row height (height:100%/align-items:stretch), quote area flexes, person row pinned to bottom. Verify on all 4 solution pages. | ✅ |
| 8 | Service template — testimonials | Avatars: replace empty discs with initials monogram (NOT AI faces) | Cole asked for AI-generated headshots. Declined: the testimonials name real people at real companies (Kristen Haney/Grove, Karen Hewitt/WeWork, Keenan Wilson/Stance) — pairing invented faces with real identities misrepresents them. Implement instead: styled initials monogram on the brand-gradient disc (KH/KH/KW), professional and standard. If real headshots can be obtained from the clients (with their OK), the avatar fields already accept attachment IDs. | ✅ |
| 9 | Interactive Content Marketing (/services/bespoke-content-experience/) | Page feels off from the new design system — align it | Bigger item: page-bespoke-content-experience.php is its own template (excluded from the data rollout, keeps its bespoke sections/interactive mockups). Restyle it onto the design system: pfx grid hero (left-aligned + in-hero wedge + accent bar), README type scale (Poppins/Assistant/Montserrat eyebrows), token colors, pfx buttons (magnetic primary), clip-path wedges between sections, sweep-hover cards, grain on dark sections, final CTA gradient+orbs, reveal staggers. Keep its unique content & interactive demos — this is a reskin, not a rebuild. Verify copy unchanged byte-for-byte. | ✅ |
| 10 | All solution pages — Reality section | Add the pull-quote treatment to the Reality area on every page | Use the design's pull-quote band styling (giant ghost " mark, Poppins clamp(24,3vw,36), cyan highlight span) in/adjacent to the Reality section. SEO page quote (Cole-authored): "Search is changing faster than businesses can keep up." — suggest highlighting "faster than businesses can keep up". Other pages: reposition their existing pull_quote lines into this slot (content-writing "quality problem…", paid "resonates with the right audience", visual "pay less for more") rather than double-rendering — remove the old lower placement if moved. CONFIRM with Cole: does "all the pages" include the industry pages' Reality/challenges area too? | ✅ |
| 11 | All solution pages — images | Same intro/process images repeat on every page — replace with per-page contextual images | Currently one shared editorial-workspace intro + one team-collab process photo across content-writing/seo/paid/visual. Fix: per-page images chosen for context — content-writing keeps editorial workspace; visual = design/creative studio work (strong candidate: real portfolio images from the media library, like the Meyer's/Reef assets used in the ecommerce gallery); seo = search/analytics context; paid = media planning/campaign context. Prefer current-site portfolio images where apt, else Unsplash stock. Bundle per-page files in page-images/, add per-page keys in setup-page-images.php (replace the shared-ID wiring), re-apply locally. SCOPE (per Cole): ALL solution AND industry pages — review every image slot on all 8 pages for contextual fit (industry challenges photos are already distinct per page, but re-evaluate each against its section copy while doing the pass). | ✅ |
| 12 | About/Team/Work | Combine Our Story + Our Team + Our Work into one About page | Single page at /about-stretch-creative/ with sections + anchors (#our-story, #our-team, #our-work). 301 /the-team/ and /our-work/ → the anchors. Nav: About becomes a single link (dropdown children removed); footer About column links point at the anchors. Design-system styling throughout; content merged from the three existing pages, copy unchanged. | ✅ |
| 13 | All solution pages — Reality section | Reality reformatted to 2-col: copy left, pull-quote card right (per Cole live request, modeled on the Ecommerce Challenges split) | Implemented directly in page-service.php: .svc-reality-grid 1fr/1.05fr, white quote card w/ gradient left bar + ghost mark + cyan highlight; centered fallback when no pull quote; standalone band only when no Reality copy. Verified on all 4 solution pages + screenshot. | ✅ |
| 4 | Contact | Map card: decide whether the stylized placeholder gets a real map background | Cole asked if it needs a map. Recommendation: keep it lightweight — add a static dark-styled map image of Victoria, BC as the card background (bundled in page-images/, sideloaded like other page images; no API keys/external requests). DONE: Cole approved a map; CARTO dark-tile static map of Victoria shipped with OSM/CARTO attribution (commit 492f5e7). | ✅ |
| 3 | Contact | Contact-form submissions should email cole@stretchcreative.co (for now) | Recipient configured in the contact-form handler (stretch-theme/inc/contact-form.php — check how the "to" address is derived, likely admin_email or a hardcoded value/option). Set to cole@stretchcreative.co; keep the leads CPT capture as-is. Note: outbound SMTP provider is still a parked decision — on prod, wp_mail deliverability isn't guaranteed until that's set up; flag delivery status to Cole after the change. | ✅ |
| 2 | Home | Visible 1px horizontal seam line inside the dark Why Trust section, right below the wedge divider from Who We Serve | Classic wedge-boundary seam: the clip-path wedge div and the following section don't overlap, leaving a sub-pixel rounding gap (hero wedge avoids this with bottom:-1px). Fix: overlap the wedge into the next section by 1px (negative margin or -1px offset) on ALL wedge dividers site-wide (home, service, industry templates) — check every light↔dark transition at multiple zoom levels. SECOND INSTANCE confirmed by Cole on content-writing at the How We Work → Why Stretch wedge — same fix covers it; verify every wedge on every page after patching. | ✅ |
| 1 | Home | Hero checkered grid background not visible — only shows in the strip behind the nav | Grid cells + lines exist in DOM (pfx-grid-container renders). Likely the `.pfx-hero-grid::after` radial vignette (rgba(26,31,46,0.95)→0.55@52%→transparent@78%, ellipse 62%×58%) is covering nearly the whole hero at wide viewports, plus cells idle at 0.15 opacity of an already-0.13-alpha fill. Fix: tune vignette size/stops and/or raise base grid-line visibility to match the design reference, where the grid reads clearly outside the center vignette. Verify against Home.dc.html + screenshots/01-home.png. | ✅ |

<!-- entries appended as Cole reports them -->
